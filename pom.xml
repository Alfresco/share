<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.alfresco</groupId>
        <artifactId>alfresco-parent</artifactId>
        <version>5.1-SNAPSHOT</version>
    </parent>
    <artifactId>alfresco-share-parent</artifactId>
    <packaging>pom</packaging>
    <name>Alfresco Share Parent</name>

    <modules>
        <module>share-services</module>
        <module>share</module>
        <module>share-po</module>
        <module>surf</module>
        <module>web-framework-commons</module>
        <module>web-editor</module>
        <module>web-editor-plugin</module>
        <module>web-editor-samples/customer-site</module>
        <module>wcmquickstart-module</module>
        <module>alfresco</module>
    </modules>

    <properties>
        <version.major>5</version.major>
        <version.minor>1</version.minor>
        <version.revision>0</version.revision>
        <dependency.aikau.version>1.0.17.1</dependency.aikau.version>
        <dependency.freemarker.version>2.3.20-alfresco-patched</dependency.freemarker.version>
        <dependency.rhino.version>1.7R4-alfresco-patched</dependency.rhino.version>
        <dependency.alfresco-web-framework-commons.version>${project.version}</dependency.alfresco-web-framework-commons.version>
        <dependency.surf.version>${project.version}</dependency.surf.version>
    </properties>

   <scm>
       <connection>scm:svn:https://svn.alfresco.com/repos/alfresco-open-mirror/web-apps/Share/trunk/</connection>
       <developerConnection>scm:svn:https://svn.alfresco.com/repos/alfresco-enterprise/web-apps/Share/trunk/</developerConnection>
   </scm>
    <repositories>
        <repository>
            <id>alfresco-public</id>
                <url>https://artifacts.alfresco.com/nexus/content/groups/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>alfresco-public-snapshots</id>
            <url>https://artifacts.alfresco.com/nexus/content/groups/public-snapshots</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
    <pluginRepositories>
        <pluginRepository>
            <!-- Only used for the customised version of yuicompressor in yuicompressor-maven-plugin -->
            <id>alfresco-public</id>
            <url>https://artifacts.alfresco.com/nexus/content/groups/public</url>
        </pluginRepository>
    </pluginRepositories>

    <build>
        <!-- Resets Maven default -->
        <sourceDirectory>src/main/java</sourceDirectory>
        <testSourceDirectory>src/test/java</testSourceDirectory>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <testResources>
            <testResource>
                 <directory>src/test/resources</directory>
            </testResource>
        </testResources> 

        <plugins>
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <skipIfEmpty>true</skipIfEmpty>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                            <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                        </manifest>
                        <manifestEntries>
                            <Specification-Version>${version.major}.${version.minor}.${version.revision}</Specification-Version>
                            <Implementation-Version>${project.version}</Implementation-Version>
                            <Build-Date>${maven.build.timestamp}</Build-Date>
                            <Build-Key>${build.key}</Build-Key>
                            <Build-Number>${build.number}</Build-Number>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                            <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                        </manifest>
                        <manifestEntries>
                            <Specification-Version>${version.major}.${version.minor}.${version.revision}</Specification-Version>
                            <Implementation-Version>${project.version}</Implementation-Version>
                            <Build-Date>${maven.build.timestamp}</Build-Date>
                            <Build-Key>${build.key}</Build-Key>
                            <Build-Number>${build.number}</Build-Number>
                            <Build-Revision>${repository.revision.number}</Build-Revision>
                        </manifestEntries>
                        <manifestSections>
                            <manifestSection>
                                <name>Source-control</name>
                                <manifestEntries>
                                    <Source-Revision>${custom.svn.revision.number}</Source-Revision>
                                    <Source-Branch-Revision>${planRepository.revision}</Source-Branch-Revision>
                                    <Source-Branch>${planRepository.branchName}</Source-Branch>
                                    <Source-Url>${planRepository.repositoryUrl}</Source-Url>
                                    <Custom-ref>${custom.svn.revision.number}</Custom-ref>
                                </manifestEntries>
                            </manifestSection>
                        </manifestSections>
                    </archive>
                    <warSourceDirectory>src/main/webapp</warSourceDirectory>
                    <attachClasses>true</attachClasses>
                    <useCache>true</useCache>
                    <recompressZippedFiles>false</recompressZippedFiles>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
