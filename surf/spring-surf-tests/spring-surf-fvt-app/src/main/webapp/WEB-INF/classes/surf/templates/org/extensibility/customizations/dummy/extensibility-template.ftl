<#-- If this template gets applied it should cause the test to fail. However, its existence tests that 
     only the correct customizations are matched. -->
<@markup id="FailTest" target="inner1" action="remove"/>
